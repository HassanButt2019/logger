void main() {
  IntegrationTestWidgetsFlutterBinding.ensureInitialized();
  testWidgets('Logger shows when added to overlay', (tester) async {
    // Add Logger to overlay, tap, verify response screen appears
  });
}
